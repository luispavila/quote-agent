# Roda local (build) e em produção na EC2 (imagem do ECR via API_IMAGE).
# Local:  docker compose up --build
# EC2:    API_IMAGE=<ecr>:<tag> docker compose up -d  (via GitHub Action → SSM)
services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN:-:80}
    volumes:
      - ./infra/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api

  api:
    image: ${API_IMAGE:-quote-agent-api:local}
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      APP_ENV: ${APP_ENV:-dev}
      WA_SERVICE_URL: http://wa-service:3001
      WA_SHARED_TOKEN: ${WA_SHARED_TOKEN:-dev-token-0123456789}
    expose:
      - "8000"

  wa-service:
    image: ${WA_IMAGE:-quote-agent-wa:local}
    build: ./wa-service
    restart: unless-stopped
    environment:
      PORT: "3001"
      WA_SHARED_TOKEN: ${WA_SHARED_TOKEN:-dev-token-0123456789}
      WEBHOOK_URL: http://api:8000/webhooks/wa
      AUTH_DIR: /data/auth
    volumes:
      - wa_auth:/data
    ports:
      - "53001:3001"  # curl -X POST localhost:53001/pairing/qr -H "x-wa-token: ..."

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-quote}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-quote}
      POSTGRES_DB: ${POSTGRES_DB:-quote}
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "55432:5432"  # porta alta p/ não colidir com o devcontainer do funniie-core
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-quote}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  caddy_data:
  caddy_config:
  pg_data:
  wa_auth:
